name: patchelf
version: "1.0"
summary: test
description: patchelf
grade: devel
confinement: classic
base: SNAPCRAFT_BASE
build-base: SNAPCRAFT_BUILD_BASE

parts:
  hello:
    source: .
    plugin: make
    build-packages:
      - gcc
      - libc6-dev
      - libcurl4-openssl-dev
    build-attributes:
      - enable-patchelf
