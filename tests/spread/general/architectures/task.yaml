summary: build a snap using architectures

execute: |
  run_snapcraft pack --platform amd64
  test -f architectures_1.0_amd64.snap
  # core24 doesn't accept multiple builds in destructive mode
  test ! -f architectures_1.0_arm64.snap
