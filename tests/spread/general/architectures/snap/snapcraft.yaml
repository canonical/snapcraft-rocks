name: architectures
version: "1.0"
summary: test
description: architectures
grade: devel
confinement: strict
base: SNAPCRAFT_BASE
build-base: SNAPCRAFT_BUILD_BASE

platforms:
  amd64:
    build-on: amd64
    build-for: amd64
  arm64:
    build-on: [amd64, arm64]
    build-for: arm64

parts:
  nil:
    plugin: nil
