name: chisel
version: "1.0"
summary: test
description: chisel
grade: devel
confinement: strict
base: SNAPCRAFT_BASE
build-base: SNAPCRAFT_BUILD_BASE

parts:
  base:
    plugin: nil
    stage-packages:
      - base-files_etc
      - base-files_bin
      - base-files_lib
      - base-files_tmp
      - base-files_var
      - base-files_home
